# RAG Pipeline Environment Variables
# Copy this file to .env and fill in your actual API keys

# DeepInfra API key for LLM-based query generation (REQUIRED)
DEEPINFRA_API_KEY=your_deepinfra_api_key_here

# Alternative LLM API key name (same as above)
LLM_API_KEY=your_deepinfra_api_key_here

# NCBI API key for PubMed (optional but recommended for higher rate limits)
# Get one at: https://www.ncbi.nlm.nih.gov/account/
NCBI_API_KEY=your_ncbi_api_key_here

# Your email address (REQUIRED for PubMed API)
# NCBI requires an email for API requests
EMAIL=your.email@example.com

# OpenAI API key (optional - if using OpenAI instead of DeepInfra)
OPENAI_API_KEY=your_openai_api_key_here

# Anthropic API key (optional - if using Claude)
ANTHROPIC_API_KEY=your_anthropic_api_key_here
